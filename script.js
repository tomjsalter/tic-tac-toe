const gameBoard = (() => {
  let board = [];
  return { board };
})();

const player = (name) => {
  return { name };
};

const displayController = (() => {
    // logic to control the flow of the game
})();